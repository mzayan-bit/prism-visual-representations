# Metrics Storage (`artifacts/metrics`)

## Purpose
Destination for raw telemetry, evaluation scorecards, CKA matrices, calibration metrics, and run summaries. Lightweight summary files (`summary.json`) can be preserved.
