# Integration Tests (`tests/integration`)

## Purpose
This directory contains integration test suites that exercise multiple PRISM modules in concert, validating end-to-end experiment pipelines, configuration parsing, data manifest verification, and evaluation runs.
