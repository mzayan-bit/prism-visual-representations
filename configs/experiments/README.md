# Experiment Configurations (`configs/experiments`)

## Purpose
Defines reproducible, end-to-end experiment recipes that bind specific models, datasets, training policies, seeds, and evaluation protocols together.
