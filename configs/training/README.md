# Training Configurations (`configs/training`)

## Purpose
Defines optimization hyperparameters (optimizers, learning rates, weight decays, schedulers, batch sizes, gradient clipping) and computational budget allocations.
