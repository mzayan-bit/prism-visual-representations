# Backend Tests

## Purpose
This directory contains backend-specific test fixtures, contract tests, and module-level test suites that test `prism.*` components.

## Organization
- Top-level tests are located in `/tests/` (unit, integration, smoke).
- Specialized internal backend tests can also reside here for backend-isolated test configurations.
