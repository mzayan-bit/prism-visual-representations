# External Data Storage (`data/external`)

## Purpose
Destination for third-party corruption datasets (e.g. ImageNet-C/CIFAR-10-C style perturbations), external probe annotations, or pre-computed embeddings from external models.
