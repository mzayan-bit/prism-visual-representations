# Processed Data Storage (`data/processed`)

## Purpose
Local destination for processed tensors, serialized index records, or canonicalized splits. All files here are strictly reproducible from raw data and config definitions.
